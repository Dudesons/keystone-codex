# MDT 6.2.13 → 6.2.15

Generated on 2026-09-05, comparing `HEAD` against the working tree.

Release notes, for the half of the analysis no tool does: https://github.com/Nnoggie/MythicDungeonTools/releases

## Summary

| Dungeon | 1 | 2 | 3 | 4 | 5 | 6 |
| --- | --- | --- | --- | --- | --- | --- |
| (index) | 0 | 0 | 0 | 0 | 0 | 1 |
| altar-of-fangs | 0 | 0 | 0 | 0 | 0 | 1 |

## What this pass did

Worked on 2026-09-05. Ticked boxes were acted on; unticked ones were read and are recorded here.

- **Severity 3 is empty by *evidence* this time, not by construction.** `FORCE=1 npm run
  fetch:assets` **was** run: all 891 spells and all 259 creatures were re-fetched in both
  languages, exit 0, and `spells.json` and `npcs.json` came back byte-identical. Read the index
  finding under severity 6 with that in mind — it says the label table "did not change" and then
  points at the forced fetch as the thing that would have moved it, which reads as if the fetch
  had been skipped. It was not. No tooltip moved under any note, and no creature label was found
  lagging behind an earlier rename.
- **`Unresolved spells: 1300666`**, unchanged from the previous run — still the one pre-existing
  id Wowhead has no page for. Nothing newly unresolved.
- **Maps: 0 rebuilt, 8 left alone.** No `textureFolder` moved.
- **The `packs:` trap did not fire.** The one real card scoping a tip to a pack is
  the-blinding-vale's Sporeblight Belcher, and this update touched no dungeon but Altar of Fangs.
- **Two clones is the whole update.** No mob gained or lost a spell, a `count:`, a `health`, a
  `cc` or a pack, in any of the eight dungeons.

### The one finding

**261554 Twinfang Harrower, 4 → 6 clones.** At 25 forces a unit its weight goes 100 → 150, and
both cards opened by spelling the old population out — "for four units" / "pour quatre unités".
Both now read six. Nothing else in either card rests on the population: the conclusion they draw
is "priced like a miniboss", which rests on the 25-per-unit price and is untouched.

Two details worth recording, neither of which changed any writing:

- **The two new clones are ungrouped** (`g: null`), standing just north of the two Venom Leech
  packs rather than inside a pull of their own. They are still counted: the extractor's total
  moved 956 → 1006 against an unchanged 817 required, and the established convention counts every
  clone regardless of grouping — King's Rest's Animated Guardian card says "Four units" for a mob
  two of whose four are ungrouped.
- **Altar of Fangs has no `codex-notes/` file**, alone among the eight dungeons, so there was no
  weight table to re-sort and no share to recompute. That is a pre-existing gap rather than
  anything this update caused, and it is left alone here rather than filled in as a side effect.

## Severity 1 — Writing already lost

The site has stopped showing this writing. Nothing else reports it.

Nothing at this severity.

## Severity 2 — Writing incomplete

A written card whose mob carries spells nobody has annotated.

Nothing at this severity.

## Severity 3 — Writing possibly stale

A tooltip moved under a note that may quote its numbers.

Nothing at this severity.

## Severity 4 — To write

New mobs and new dungeons, with no card yet.

Nothing at this severity.

## Severity 5 — Dead weight

Cards whose mob left MDT. Nothing breaks; the repository misstates itself.

Nothing at this severity.

## Severity 6 — Informational

What moved in the data, with no writing lost, though a few of these still name a follow-up.

### (index)

- [ ] **spell label table** — did not change between these two revisions, so nothing at severity 3 could be found
      → FORCE=1 npm run fetch:assets is what re-fetches tooltips; a game patch, not an MDT update, is what moves them

### altar-of-fangs

- [x] **261554 Twinfang Harrower** — clone count changed
      4 -> 6 clones
